  <%- model_class = Seller -%>
  <div class="page-header">
    <h1><%= @seller.name%> : <%= @seller.age %> tuổi </h1>
  </div>

<div class="row">
  <% @seller.products.each do |product| %>
  <div class="col-md-4">
    <div class="ui card">
      <div class="image">
        <%= image_tag product.image %>
      </div>
      <div class="content">
        <a class="header"><%= link_to product.name, product_path(id: product.id) %></a>
        <div class="meta">
          <span class="date"><%= product.price %></span>
        </div>
        <div class="description">
          <%= product.description %>
        </div>
      </div>
    </div>
  </div>
  <% end %>
</div>
<br>
<br>

  <%= link_to t('.back', :default => t("helpers.links.back")),
                sellers_path, :class => 'btn btn-default'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
                edit_seller_path(@seller), :class => 'btn btn-default' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                seller_path(@seller),
                :method => 'delete',
                :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                :class => 'btn btn-danger' %>
